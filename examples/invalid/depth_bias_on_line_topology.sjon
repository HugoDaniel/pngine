; Invalid: :depth-bias on a line topology. Depth bias is computed from a
; triangle's depth slope, so WebGPU requires depthBias, depthBiasSlopeScale
; and depthBiasClamp to all be 0 for point-list, line-list and line-strip.
;
; It crosses two sections of one pipeline -- (primitive :topology …) and
; (depth-stencil …) -- so no per-form mechanism can hold it.
(shader-module :name code :code """
@vertex fn vs(@builtin(vertex_index) i : u32) -> @builtin(position) vec4f {
  var p = array<vec2f, 3>(vec2f(-1.0, -1.0), vec2f(3.0, -1.0), vec2f(-1.0, 3.0));
  return vec4f(p[i], 0.0, 1.0);
}
@fragment fn fs() -> @location(0) vec4f { return vec4f(1.0); }
""")
(texture :name depth :size canvas :format depth24plus :usage [render-attachment])
(render-pipeline :name pipe :layout auto
  (vertex :module code :entry vs)
  (fragment :module code :entry fs
    (target :format preferred-canvas-format))
  (primitive :topology line-list)
  (depth-stencil :format depth24plus :depth-write-enabled true :depth-compare less :depth-bias 1))
(render-pass :name draw :pipeline pipe
  (color-attachment :view context-current-texture
    :clear-value [0 0 0 1] :load-op clear :store-op store)
  (depth-stencil-attachment :view depth
    :depth-clear-value 1.0 :depth-load-op clear :depth-store-op store)
  (draw :vertex-count 3))
(frame :name main :perform [draw])
