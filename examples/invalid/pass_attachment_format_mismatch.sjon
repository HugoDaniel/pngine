; Invalid: the pass renders into colorTex (rgba16float) but the pipeline's
; color target 0 is rgba8unorm. Attachment and target formats must match
; exactly, not just by class, so the pass/pipeline check rejects it.
(shader-module :name code :code """
@vertex fn vs(@builtin(vertex_index) i : u32) -> @builtin(position) vec4f {
  var p = array<vec2f, 3>(vec2f(-1.0, -1.0), vec2f(3.0, -1.0), vec2f(-1.0, 3.0));
  return vec4f(p[i], 0.0, 1.0);
}
@fragment fn fs() -> @location(0) vec4f { return vec4f(1.0); }
""")
(texture :name colorTex :size canvas :format rgba16float :usage [render-attachment])
(render-pipeline :name pipe (layout auto)
  (vertex (module code) (entry vs))
  (fragment (module code) (entry fs)
    (targets (target :format rgba8unorm))))
(render-pass :name draw :pipeline pipe
  (color-attachment :view colorTex
    :clear-value [0 0 0 1] :load-op clear :store-op store)
  (draw :vertex-count 3))
(frame :name main :perform [draw])
